name: example
description: Bloc side effect example

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  bloc: ^8.0.0
  flutter_bloc: ^8.0.0
  freezed_annotation: ^2.4.1
  bloc_side_effect:
    path: ../

dev_dependencies:
  freezed: ^2.4.7
  flutter_lints: ^1.0.0
  build_runner: ^2.1.8

flutter:
  uses-material-design: true
